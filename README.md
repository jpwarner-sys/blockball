# Blockball Dungeon

**Note on subdomain hosting**: The app dynamically scopes its manifest and `localStorage` keys to its current directory path. This prevents collisions if you host multiple games (or multiple branches of this game) on the same shared origin (like a `github.io` subdomain).
